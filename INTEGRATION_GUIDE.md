/**
 * Интеграция emoji:id() синтаксиса в Telegram bot
 * 
 * Это примеры как добавить красивые эмодзи в существующий код бота
 */

// ============================================================
// ШАГИ ИНТЕГРАЦИИ
// ============================================================

/*
1️⃣ ИМПОРТ (в начало файла)
   const { sendWithEmojiSyntax } = require('./app/premium_emojis');

2️⃣ ЗАМЕНА (везде где есть ctx.reply())
   // Было:
   await ctx.reply('Ваше сообщение');
   
   // Стало:
   await sendWithEmojiSyntax(ctx, 'Ваше сообщение emoji:id(ID)');

3️⃣ ИСПОЛЬЗОВАНИЕ
   // Используй ID эмодзи вместо них самих:
   emoji:id(5328145443106873128) - вместо красивого эмодзи
*/

// ============================================================
// ИНТЕГРАЦИЯ В КОМАНДЫ
// ============================================================

// БЫЛО:
// bot.command('start', async (ctx) => {
//   await ctx.reply('Добро пожаловать в бот!');
// });

// СТАЛО:
// const { sendWithEmojiSyntax } = require('./app/premium_emojis');
// bot.command('start', async (ctx) => {
//   await sendWithEmojiSyntax(ctx, `
// Добро пожаловать в бот! emoji:id(5325885244695425655)

// emoji:id(5328145443106873128) Команды
// emoji:id(5328146574935768874) Помощь
// emoji:id(5312949421381177815) О боте
//   `);
// });

// ============================================================
// ИНТЕГРАЦИЯ В КОЛЛБЕКИ
// ============================================================

// БЫЛО:
// bot.action('help', async (ctx) => {
//   await ctx.reply('Вот команды бота...');
// });

// СТАЛО:
// bot.action('help', async (ctx) => {
//   await sendWithEmojiSyntax(ctx, `
// emoji:id(5328146574935768874) Команды бота

// /help - Справка emoji:id(5328145443106873128)
// /menu - Меню emoji:id(5312948834137047286)
// /status - Статус emoji:id(5312949421381177815)
//   `);
// });

// ============================================================
// ИНТЕГРАЦИЯ В ON_HANDLERS
// ============================================================

// БЫЛО:
// bot.on('new_chat_members', async (ctx) => {
//   const member = ctx.message.new_chat_members[0];
//   await ctx.reply(`Добро пожаловать, ${member.first_name}!`);
// });

// СТАЛО:
// bot.on('new_chat_members', async (ctx) => {
//   const member = ctx.message.new_chat_members[0];
//   await sendWithEmojiSyntax(ctx, `
// Добро пожаловать, ${member.first_name}! emoji:id(5325885244695425655)

// Это место где:
// emoji:id(5328145443106873128) Весело
// emoji:id(5328146574935768874) Дружно
// emoji:id(5312949421381177815) Интересно
//   `);
// });

// ============================================================
// ИНТЕГРАЦИЯ В MIDDLEWARE
// ============================================================

// БЫЛО:
// bot.use(async (ctx, next) => {
//   console.log('Сообщение получено');
//   if (ctx.message && ctx.message.text.includes('/help')) {
//     await ctx.reply('Вот как использовать бота');
//   }
//   await next();
// });

// СТАЛО:
// bot.use(async (ctx, next) => {
//   console.log('Сообщение получено');
//   if (ctx.message && ctx.message.text.includes('/help')) {
//     await sendWithEmojiSyntax(ctx, `
// emoji:id(5328146574935768874) Как использовать бота:

// 1. Введи /menu emoji:id(5312948834137047286)
// 2. Нажми кнопку emoji:id(5325885244695425655)
// 3. Готово! emoji:id(5328147729537945974)
//     `);
//   }
//   await next();
// });

// ============================================================
// ИНТЕГРАЦИЯ С ВСТРОЕННЫМИ КЛАВИАТУРАМИ
// ============================================================

// БЫЛО:
// await ctx.reply('Выбери действие:', {
//   reply_markup: {
//     inline_keyboard: [
//       [{ text: 'Параметр 1', callback_data: 'param1' }],
//       [{ text: 'Параметр 2', callback_data: 'param2' }],
//     ],
//   },
// });

// СТАЛО:
// await sendWithEmojiSyntax(ctx, 'Выбери действие emoji:id(5328146574935768874):', {
//   reply_markup: {
//     inline_keyboard: [
//       [{ text: 'emoji:id(5328145443106873128) Сердце', callback_data: 'heart' }],
//       [{ text: 'emoji:id(5325885244695425655) Кот', callback_data: 'cat' }],
//       [{ text: 'emoji:id(5312948834137047286) Цветок', callback_data: 'flower' }],
//     ],
//   },
// });

// ============================================================
// ИНТЕГРАЦИЯ В错误 ОБРАБОТКУ
// ============================================================

// БЫЛО:
// try {
//   // код
// } catch (e) {
//   await ctx.reply('Ошибка: ' + e.message);
// }

// СТАЛО:
// try {
//   // код
// } catch (e) {
//   await sendWithEmojiSyntax(ctx, `
// emoji:id(5312949421381177815) Ошибка!

// ${e.message}

// Попробуй еще раз или напиши /help emoji:id(5328146574935768874)
//   `);
// }

// ============================================================
// ID ЭМОДЗИ СПРАВОЧНИК
// ============================================================

const EMOJI_REFERENCE = {
  // Сердца
  heart_purple: '5328145443106873128',    // 💜 Фиолетовое сердце
  heart_pink: '5312949421381177815',      // 🩷 Розовое сердце
  heart_fire: '5328151038599003814',      // ❤️‍🔥 Горящее сердце

  // Цветы
  flower_pink: '5312948834137047286',     // 🌸 Розовый цветок
  flower_blue: '5312947055836119424',     // 🌹 Синий цветок

  // Лица
  cat_happy: '5325885244695425655',       // 😸 Счастливый кот
  angel: '5328147729537945974',           // 😇 Ангел
  star_purple: '5328146574935768874',     // ⭐ Фиолетовая звезда
};

// ============================================================
// ГОТОВЫЕ ТЕКСТОВЫЕ БЛОКИ С ЭМОДЗИ
// ============================================================

const EMOJI_MESSAGES = {
  welcome: `
Добро пожаловать! emoji:id(5325885244695425655)

Это бот с премиум эмодзи emoji:id(5328146574935768874)
  `.trim(),

  help: `
emoji:id(5328146574935768874) СПРАВКА

/menu - Главное меню emoji:id(5312948834137047286)
/help - Эта справка emoji:id(5328145443106873128)
/about - О боте emoji:id(5312949421381177815)
  `.trim(),

  error: `
emoji:id(5312949421381177815) Что-то пошло не так

Попробуйте позже emoji:id(5328147729537945974)
  `.trim(),

  success: `
emoji:id(5328147729537945974) Успешно!

Готово emoji:id(5328146574935768874)
  `.trim(),
};

// ============================================================
// ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
// ============================================================

/*
// ПРИМЕР 1: Основная команда
bot.command('start', async (ctx) => {
  await sendWithEmojiSyntax(ctx, EMOJI_MESSAGES.welcome);
});

// ПРИМЕР 2: Команда с кнопками
bot.command('menu', async (ctx) => {
  await sendWithEmojiSyntax(ctx, 'Меню emoji:id(5312948834137047286):', {
    reply_markup: {
      inline_keyboard: [
        [{ text: 'emoji:id(5328145443106873128) Вариант 1', callback_data: 'opt1' }],
        [{ text: 'emoji:id(5325885244695425655) Вариант 2', callback_data: 'opt2' }],
      ],
    },
  });
});

// ПРИМЕР 3: Обработка коллбека
bot.action('opt1', async (ctx) => {
  await sendWithEmojiSyntax(ctx, EMOJI_MESSAGES.success);
});

// ПРИМЕР 4: Обработка ошибок
bot.catch(async (err, ctx) => {
  console.error('Ошибка:', err);
  await sendWithEmojiSyntax(ctx, EMOJI_MESSAGES.error);
});
*/

// ============================================================
// МИГРАЦИЯ СУЩЕСТВУЮЩИХ СООБЩЕНИЙ
// ============================================================

/*
ШАГ 1: Открыть bot.js и найти все ctx.reply()

ШАГ 2: Для каждого ctx.reply('текст') заменить на:
        await sendWithEmojiSyntax(ctx, 'текст emoji:id(ID)');

ШАГ 3: Выбрать подходящие ID эмодзи из справочника выше

ШАГ 4: Тестировать

ШАГ 5: Развернуть
*/

module.exports = {
  EMOJI_REFERENCE,
  EMOJI_MESSAGES,
};
